---
apiVersion: v1
kind: Service
metadata:
  name: resource-service
  namespace: dev-marvel
spec:
  type: NodePort
  selector:
    app: resource-service
  ports:
    - protocol: TCP
      port: 8086
      nodePort: 30002
      name: web-server
    - protocol: TCP
      port: 5005
      nodePort: 31000
      name: debug

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: resource-service
  namespace: dev-marvel
spec:
  replicas: 2
  selector:
    matchLabels:
      app: resource-service
  template:
    metadata:
      labels:
        app: resource-service
    spec:
      containers:
        - name: resource-service
          image: johnsallison/resource-service:0.1
          env:
            - name: PG_USER
              valueFrom:
                secretKeyRef:
                  name: persistence-secrets
                  key: resource_db_user
            - name: PG_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: persistence-secrets
                  key: resource_db_password
          volumeMounts:
            - name: resource-service-app-config
              mountPath: /config
          livenessProbe:
            httpGet:
              port: 8086
              path: /actuator/health
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 2
          readinessProbe:
            httpGet:
              port: 8086
              path: /actuator/health
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 2
      volumes:
        - name: resource-service-app-config
          configMap:
            name: resource-service-config-map
            items:
              - key: application.yml
                path: application.yml
